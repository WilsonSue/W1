// const bcrypt = require("bcrypt");
// const plainPassword = "pass1";

// const encryptedPassword = bcrypt.hashSync(plainPassword, 10);
// console.log(encryptedPassword);
// console.log(bcrypt.compareSync("pass1", encryptedPassword)); // true
